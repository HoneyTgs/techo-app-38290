<h1>ユーザー情報</h1>
<div class="main">
  <%= @name %>さん
</div>
<div class="contents row">
  <p>投稿一覧</p>
  <% @plans.each do |plan| %>
    <p><%= link_to plan.title, plan_path(plan.id) %></p>
  <% end %>
</div>  
<div class='footer'>
  <%= link_to 'もどる', root_path %>
</div>